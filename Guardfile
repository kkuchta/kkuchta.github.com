guard 'haml', input:'_layouts/haml', output:'_layouts' do
    watch(%r{^_layouts/haml.+\.haml$})
end
guard 'haml', input:'_includes/haml', output:'_includes' do
    watch(%r{^_includes/haml.+\.haml$})
end
